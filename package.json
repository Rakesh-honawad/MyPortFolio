{
    "name": "my-fullstack-project",
    "private": true,
    "scripts": {
        "dev": "npm run dev --prefix client"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    },
    "dependencies": {
        "dotenv": "^17.2.1",
        "nodemailer": "^7.0.5"
    }
}
